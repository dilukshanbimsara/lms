// ─── Role Enum ───────────────────────────────────────────────
// Use this as the single source of truth for roles across the app.

export enum Role {
  SUPER_ADMIN = 'SUPER_ADMIN',
  TEACHER = 'TEACHER',
  STUDENT = 'STUDENT',
}
